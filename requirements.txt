# ==========================================
# JOBEZEE — requirements.txt
# Install: pip install -r requirements.txt
# ==========================================

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0           # Neon / PostgreSQL async driver
psycopg2-binary>=2.9.9    # sync fallback (Alembic migrations)

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT
bcrypt>=4.0.0                      # password hashing (used directly — passlib incompatible with bcrypt 4+)
python-multipart>=0.0.9            # file upload / form parsing

# ── Settings / Config ─────────────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── Phase 1 — Core HTTP / Parsing ────────────────────────────────────────────
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
markdownify>=0.11.6
regex>=2023.12.25
pyyaml>=6.0.1

# ── Phase 1 — Job Board Scraping ──────────────────────────────────────────────
# IMPORTANT: Install manually with --no-deps (numpy version conflict):
#   pip install --no-deps python-jobspy
python-jobspy

# ── Phase 1 — Browser Automation (SmartExtract) ───────────────────────────────
playwright>=1.42.0
# After install run: playwright install chromium

# ── Phase 1 — LLM Providers (install at least one key) ───────────────────────
google-generativeai>=0.3.0
openai>=1.30.0

# ── Data Export ───────────────────────────────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.2

# ── Resume text extraction (for job-card tailoring) ───────────────────────────
pdfplumber>=0.11.0
python-docx>=1.1.0

# ── Auto-Apply (Phase 3 — applypilot) ─────────────────────────────────────────
mcp>=1.0           # MCP client for Playwright browser automation
typer>=0.9.0       # CLI framework used by applypilot
rich>=13.0         # Terminal UI (progress, dashboard)
2captcha-python>=1.2.0  # CAPTCHA solving via 2Captcha API
